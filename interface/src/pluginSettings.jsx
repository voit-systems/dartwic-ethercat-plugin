import React from "../sdk/react.ts";

export function ExamplePluginSettings() {
    return (
        <div className="rounded-lg border border-border bg-card p-4 text-sm text-foreground">
            Example interface plugin settings are loaded.
        </div>
    );
}
